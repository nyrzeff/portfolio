import { Layout, Intro, Stack, ProjectContent, Contact } from "@components";

export function App() {
    return (
        <Layout>
            <Intro />
            <Stack />
            <ProjectContent />
            <Contact />
        </Layout>
    );
}
